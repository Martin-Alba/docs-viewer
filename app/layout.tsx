import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Visor de Documentos - PDF, DOC, DOCX",
  description: "Aplicación web para cargar y visualizar documentos PDF, DOC y DOCX de forma sencilla y rápida.",
  keywords: ["visor", "documentos", "PDF", "DOC", "DOCX", "visualizar", "cargar archivos"],
  authors: [{ name: "Document Viewer App" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
